<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bootstrap demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  </head>
  <body class="d-flex flex-column w-100 align-items-center justify-content-center bg-dark text-light">
    <h1>Productos</h1>

    <% if (mockData.length > 0) { %> 
    <table class="table table-dark table-striped-columns w-50">
        <thead>
          <tr>
            <th scope="col">Title</th>
            <th scope="col">Price</th>
            <th scope="col">Thumbnail</th>
          </tr>
        </thead>
        <tbody>
          <% mockData.forEach( item => { %>
            <tr>
              <th scope="row"><%= item.title %></th>
              <td><%= item.price %> </td>
              <td>
                <img src=<%= item.thumbnail %> width="40px" height="40px"></img>
              </td>
            </tr>
          <%})%>
        </tbody>
      </table>
      <% } else { %>
        <p>No hay productos en la canasta</p>
      <% }%>
      <a href="/" class="btn btn-primary">Cargar Datos</a> 
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
  </body>
</html>